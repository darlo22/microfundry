{
  "version": 2,
  "buildCommand": "ls -la && pwd && cd client && ls -la && npm install && npm run build && cd .. && mkdir -p dist && cp -r client/dist/* dist/ && esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist",
  "outputDirectory": "dist",
  "installCommand": "npm install"
}
